// src/runtime/visual.mjs — A-class tool: describe screenshot (OCR/vision stubbed)
export async function describeVisual({ path } = {}) { return { ok: true, note: 'vision backend pending', path: path ?? null }; }
